{
  "name": "js-playground",
  "version": "0.0.1",
  "description": "This is the javascript playground where we will write some interesting JS code",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "nodemon-index": "nodemon ./index.js"
  },
  "author": "Shripada",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^2.0.20"
  },
  "dependencies": {
    "@paralleldrive/cuid2": "^2.2.0",
    "assert": "^2.0.0",
    "prompt-sync": "^4.2.0"
  }
}
